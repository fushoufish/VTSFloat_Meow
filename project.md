# VTSFloat_Meow 项目说明

## 1. 项目定位

VTSFloat_Meow 是一个 Windows 原生桌面叠加层，用于接收 VTube Studio 通过 Spout2
发送的透明模型画面，并将其显示在游戏或其他窗口上方。

当前版本：**v1.1 正式版**。

项目不向游戏注入代码，也不依赖 Python 或 Qt。VTube Studio 负责渲染模型并发送
共享纹理，VTSFloat_Meow 负责接收、缩放、合成透明画面并显示。

## 2. 技术栈

- C++17
- Win32 窗口与输入 API
- Direct3D 11 / Spout2
- `UpdateLayeredWindow` 分层透明窗口
- CMake + Visual Studio 2022

## 3. 画面管线

```text
VTube Studio
    -> Spout2 共享纹理
    -> D3D11 接收（发送端 GPU）
    -> 必要时 GPU 缩放
    -> 预乘 BGRA DIB
    -> UpdateLayeredWindow
    -> 桌面/游戏上方的透明窗口
```

### 3.1 接收

程序查找 VTube Studio 的 Spout 发送端，读取共享纹理尺寸、格式和发送端 GPU。
接收端尽量使用同一块 GPU，避免跨 GPU 共享纹理失败或产生额外复制。

### 3.2 缩放与比例

当窗口尺寸与 Spout 画布不一致时，程序在 GPU 上完成缩放，再将结果复制到常驻的
BGRA 缓冲区。比例锁定时保持源画布比例，自由模式允许横向或纵向单独拉伸。

### 3.3 透明显示

模型画面使用预乘 Alpha，窗口通过 Win32 分层窗口提交到桌面合成器。边框、工具栏
和调试信息属于独立的 UI 图层，不参与模型透明度计算。

## 4. 主要功能

- Spout2 透明画面接收
- GPU 自动匹配与多 GPU 选择
- 固定帧率、跟随显示器刷新率和自定义帧率
- 窗口移动、缩放、多屏切换和比例锁定
- 边框颜色、渐变模式和粗细设置
- 模型透明度与悬停透明化
- 调试性能信息和 FPS 曲线
- 系统托盘、快捷键和位置/设置自动保存
- VTube Studio 启动辅助

## 5. 性能设计

渲染线程只处理接收、缩放、合成和上屏；窗口交互、工具栏和设置使用独立的 UI
路径。常驻缓冲区避免每帧分配大块内存，日志默认不记录高频渲染指标。

性能栏中的接收、缩放、上屏耗时用于定位瓶颈，不代表 VTube Studio 的内部渲染
帧率。VTube Studio 的 VSYNC 或帧率设置会限制发送端更新频率。

## 6. 早期瓶颈与解决方向

### Python/Qt 主线程排队

旧方案将接收、绘制和窗口消息集中在 GUI 主线程，游戏高帧率或窗口消息繁忙时会
产生队列堆积。当前方案改为原生 C++ 渲染路径。

### SwapChain/Present 阻塞

早期 DirectComposition 与 DXGI SwapChain 路径在无上限帧游戏中可能被桌面合成器
长时间阻塞。当前默认路径改用 `UpdateLayeredWindow`，避免等待交换链 Present。

### CPU 缩放和逐像素处理

逐像素 CPU 缩放会随窗口尺寸增加而放大开销。当前优先使用 D3D11 采样，再进行
一次必要的缓冲区提交。

### 多 GPU 共享限制

Spout 共享纹理通常要求接收端与发送端使用同一 GPU。程序会显示检测到的 GPU，并将
自动模式优先指向节能/核显；手动切换到另一块 GPU 时需要重新启动相关程序。

### 无上限帧率调度压力

游戏无上限运行时可能挤压桌面合成、浏览器和 VTube Studio 的线程。将叠加层帧率
设为 VTS 或显示器的固定刷新率，通常比盲目提高目标帧率更稳定。

## 7. 构建

在安装 Visual Studio 2022 C++ 工具链的 Windows 环境中运行：

```powershell
.\native\build_native.ps1 -Configuration Release
```

输出为仓库根目录的 `VTSFloat_Meow.exe`。构建目录和编译产物不应提交到仓库。

## 8. 使用前提

1. 启动 VTube Studio。
2. 在 VTube Studio 中开启 Spout2，并设置透明背景。
3. 启动 `VTSFloat_Meow.exe` 或 `start_overlay.cmd`。
4. 如出现黑屏或无法接收，先确认发送端名称、GPU 选择和 VTube Studio 是否正在运行。

## 9. 目录说明

```text
VTSFloat_Meow.exe          可直接运行的程序
native/                    C++ 源码、CMake 配置和 Spout2 源码
md/                        面向读者的原理与性能说明
README.md                  快速介绍与使用说明
start_overlay.cmd          启动便捷脚本
stop_overlay.cmd           关闭便捷脚本
```

个人调试记录、绝对路径、运行日志和本地缓存不属于公开项目文档。

## 10. 尚未写入公开 README 的开发细节

本节是给维护者使用的实现记录。它比公开 README 更接近代码层，涉及状态机、线程边界和
配置持久化；不要直接复制到面向普通用户的说明中。

### 10.1 VTube Studio API 连接状态机

API 线程不会因为程序启动就立刻弹出错误提示。启动后先等待 Spout 收到有效模型帧，模型
画面稳定约 2.5 秒后才开始第一次 API 探测。这样可以避免 VTS 尚未启动时的误报。

连接顺序如下：

```text
有效 Spout 模型帧
    -> 等待 2.5 秒
    -> 尝试 INI 中保存的端口
    -> 失败后探测默认/扫描到的 VTS 端口
    -> WebSocket APIStateRequest 验证
    -> 使用缓存 token，失败时请求新 token
    -> VTS 中显示插件授权后完成认证
```

首次探测会使用较宽的 I/O 超时（默认端口 8001 为 3 秒，其余扫描端口为 1 秒），因为
新设备、VTS 刚启动或系统负载较高时，短超时容易造成“扫描多次才成功”。认证成功后，
当前端口写回 `%LOCALAPPDATA%\\vts_overlay_layered.ini`；断线时仅以 600 ms 间隔静默重试
已保存端口，不重复刷出弹窗。已认证会话允许少量连续轮询超时，避免一次繁忙帧就被误判为
API 断开。

API 线程独立于渲染线程。统计请求约每 2 秒一次，表情命令使用约 50 ms 的命令泵，避免
为了提高表情响应而提高统计请求频率。关闭 VTS 时，主窗口状态机会回到“等待 VTube Studio”
并自动解除锁定，防止透明覆盖层继续拦截鼠标。

### 10.2 表情状态与悬停预览

VTube Studio 允许同时启用多个表情，不能只保存一个“当前表情”。个性化界面打开时会先
请求一次 `ExpressionStateRequest`，记录当时所有 active 表情，作为恢复基线；界面打开期间
以约 600 ms 的低频轮询检查用户是否在 VTS 中自行切换了表情。检测到外部变化后，恢复基线
会更新为最新状态，避免覆盖用户刚刚做出的选择。

每一次鼠标进入悬停范围都必须先请求一份带新版本号的 `ExpressionStateResponse`。渲染路径只
发出异步请求，在新快照返回前不发送播放命令，因此“读取播放前状态”的优先级始终高于表情
渐入。播放期间另以约 250 ms 的频率读取状态：程序保存一份由自身播放命令产生的预期状态，
只有实际状态与预期状态不同才视为用户在 VTS 中产生了新的状态 N。新的状态 N 会替换本次
悬停的恢复基线（状态 1 → 状态 2），并同步更新个性化会话基线，但不会修改用户在程序中勾选
的悬停表情列表。

重复进入采用 2 秒重触发冷却。当前表情尚未恢复时，快速移出再移入只会取消恢复倒计时，
不会重复开启播放会话；冷却结束后发生真实的“移出 → 再移入”边沿时，程序会额外请求一份
版本更新的 `ExpressionStateResponse`，并继续用预期状态过滤自身正在播放的表情。完成一次会话
后若在冷却期内进入，该次进入会被忽略；即使鼠标一直停留到冷却结束也不会自动请求，必须
再次移出并进入，避免高频进出造成 API 请求堆叠或把程序自身的临时表情写进恢复基线。

预览流程为：

1. 鼠标移入后先读取并记录完整的播放前状态；拿不到可靠快照时不播放。
2. 读取成功后通过 `ExpressionActivationRequest` 以约 0.3 秒渐入所选表情。
3. 预览保持设定时长；默认悬停停留时间为 1 秒，个性化面板的显式预览等待时间为 2.5 秒。
4. 鼠标离开触发范围后，按恢复延时发送关闭命令，并以约 1 秒渐出。
5. 结束或取消个性化面板时，逐个恢复进入面板前保存的所有表情，而不是只关闭最后一个。

命令队列会按表达式文件名折叠重复命令，并立即更新本地状态缓存。这样快速“移入 → 移出 →
再次移入”时，旧的关闭命令不会在新命令之后到达，避免表情卡在已装载状态。界面显示名称会
去掉 `.exp3`、`.json` 和 `.exp3.json` 后缀，但向 API 请求时仍使用原始文件名。

### 10.3 手动多区域悬停范围

当前不再使用每帧自动寻找“最大连续物体”的实验算法。用户点击“手动框选悬停触发范围”后，
程序暂停正常编辑输入并打开覆盖选择层，使用多边形套索方式连续框选多个独立区域：

- 双击或点击起点完成当前多边形，之后可以继续绘制下一个区域；
- Enter/“完成”保存全部区域，Esc 取消；
- Ctrl+Z、Ctrl+Y 或工具栏撤销/恢复最多保留 128 个编辑快照；
- 当前完成的主体数量显示在选择工具栏中。

已完成区域会被栅格化为压缩 bit mask，并与归一化位置和宽高一起写入 INI。运行时只在需要
悬停判定时读取缓存 mask，不再执行已经取消的自动主体检测算法。窗口缩放时，mask 会按当前
窗口尺寸映射；角色在 VTS 画布内移动后，用户需要重新框选。选择预览使用绿色边界层，且不会
把调试文字、边框像素算入角色区域。

### 10.4 透明度、扩展范围和预览图层

模型透明度、悬停透明度和悬停扩展是三个独立状态。个性化预览会先复制一份原始 BGRA 缓冲，
只对模型像素改 Alpha，边框、调试性能栏和提示层保持不透明；预览结束后直接恢复原始缓冲，
避免多次叠加 Alpha 造成画面越来越暗。

扩展范围只在鼠标移入模型或编辑扩展时显示，并使用独立 Alpha 做渐入/渐出，约 2.4 秒无操作
后隐藏。关闭“鼠标移入时触发透明化”后，扩展填充和悬停预览都必须停止，不能只关闭最终
透明度。这是此前“取消勾选仍出现红色扩展”的修复约束。

### 10.5 启动辅助与 VTS 路径发现

程序会在常见 Steam 库路径中搜索 `VTube Studio.exe`，并区分普通启动与
`start_without_steam.bat`。用户可以在等待界面点击“添加启动路径”，选择文件夹或 exe；
程序会向下搜索 VTS 安装目录，只有检测到 `start_without_steam.bat` 时才显示“从外部启动 VTS”，
并把结果保存到 INI。手动路径优先于自动发现路径。

等待界面属于主窗口内部绘制的状态页，不是额外弹窗。只有在确认已收到有效模型画面后，API、
透明背景和高性能 GPU 等提示才会出现；授权提示则仅在 API 首次连接后显示。

### 10.6 配置、日志和崩溃记录

配置文件位于 `%LOCALAPPDATA%\\vts_overlay_layered.ini`，按功能分为窗口、GPU、opacity、
expression、VtsApi 等节。写入采用 Win32 INI 接口，设置在拖动、滑块实时预览或点击“完成”
时更新；“清除缓存并重置脚本”会停止 API 线程、删除 token/端口/外观和悬停区域状态，恢复
内存默认值，但不触碰 VTS 安装目录和用户模型文件。

运行日志 `%LOCALAPPDATA%\\vts_overlay_layered.log` 只记录启动、设备、API、工具栏和错误事件。
逐帧 `receive/update/paint` 性能数据不再高频写入文件，仅在调试界面和必要的诊断采样中显示。
异常处理器另写 `VTSFloat_Meow_crash.log`，用于记录原生崩溃位置；两者都不应提交到仓库。

### 10.7 当前线程边界

| 线程/路径 | 责任 | 约束 |
| --- | --- | --- |
| Win32 主线程 | 窗口消息、工具栏、托盘、设置面板 | 不能执行阻塞式 Spout/API 网络操作 |
| 渲染循环 | 接收新帧、GPU 缩放、BGRA 合成、`UpdateLayeredWindow` | 不分配大型临时缓冲，不等待 API 响应 |
| VTS API 线程 | WebSocket 认证、统计、表情状态和命令 | 通过互斥量/原子变量与渲染线程交换快照 |
| 选择层 | 多边形输入、撤销/恢复和 mask 保存 | 只在用户主动重新框选时运行 |

后续修改应优先保持这条边界：渲染线程不能调用等待时间不可控的 API，API 线程不能直接操作
窗口句柄，UI 只消费快照并发出命令。

### 10.8 多语言预览分支

多语言工作在 `feature/language` 分支进行，正式版 `main` 在验证完成前保持不变。该分支默认
输出 `VTSFloat_Meow_LanguagePreview.exe`，使用独立的窗口类、单实例互斥量和
`%LOCALAPPDATA%\vts_overlay_language_preview.ini`，因此可以与正式版并存，也不会覆盖正式版设置。

界面语言保存为 `[ui] language`，内置支持 `zh-CN`、`en`、`ja`、`ko`、`ru`，并支持值为
`custom` 的外部语言。没有保存值时根据 Windows 用户区域自动选择；用户可从工具栏固定显示的
`Language` 菜单即时切换。内置翻译缺失时回退到简体中文，不能返回空字符串。字体按书写系统
选择：中文使用 Microsoft YaHei UI，日文使用 Yu Gothic UI，韩文使用 Malgun Gothic，英文和
俄文使用 Segoe UI。

程序首次运行会在可执行文件旁生成 UTF-8 BOM 编码的
`VTSFloat_Meow.custom-language.ini`；只读目录则回退到 `%LOCALAPPDATA%`。模板以英文为对照和
缺省值，`[language]` 可修改菜单名称与字体，`[translations]` 中只允许修改等号与行尾
` #英文备注` 之间的译文，左侧 `T_` 稳定键不能改名；备注仅供对照，不会显示。`\\n`、`\\s`、
`\\\\` 分别表示换行、首尾空格和反斜杠；译文本身需要“空格 + #”时可写成 ` \\#`。缺失或空白译文
自动回退英文；新版新增文字只向旧模板追加英文项，不覆盖用户已经完成的翻译。语言菜单中的
“编辑自定义语言配置…”用于打开文件，重新选择自定义语言后会从磁盘热重载。

`native/check_localization.ps1` 会在每次原生构建前扫描全部运行时界面源码，阻止缺少翻译条目、
重复翻译键和未包装的中文界面文字进入构建。`native/localization_layout_audit.cpp` 则以实际 GDI
字体宽度检查工具栏、等待页、个性化长标签、表情选择器和手动框选工具栏等固定宽度控件。
新增或修改译文后执行 `native/build_native.ps1`；脚本会自动依次运行源码完整性检查、正常
Release 编译和 `vts_localization_audit.exe`，避免只凭字符数判断而漏掉俄文、英文的真实溢出。

### 10.9 当前渲染与持久化优化

正常渲染路径在尺寸一致时也使用 D3D11 像素着色器完成预乘 Alpha，不再由 CPU 对整张画面逐
像素转换。GPU 缩放输出使用两个 staging 纹理轮换：当前帧复制到一个纹理，同时读取上一帧的
纹理，避免每帧刚提交 `CopyResource` 就立即阻塞等待同一个资源。GPU 初始化失败时仍保留原有
CPU 转换路径作为兼容回退。

手动悬停 mask 只有在重新框选、恢复默认或取消个性化并还原旧范围时才标记为脏数据；其他滑块
和设置保存不再重复向 INI 写入整块 mask。mask 分块读取上限已从不足以容纳 4K 的旧值放宽到
可覆盖 8K 的安全范围。调试性能层复用文字 Alpha 修复缓冲，避免每行、每帧重复申请内存。
持久日志只记录状态变化与错误，VTS 未运行时的静默 API 重连不再反复写“disconnected”，并在
下次启动时将超过 4 MiB 的单一日志就地清空，避免产生额外轮转文件。

“跟随 VTS 配置/渲染”模式已经从源头按 VTS 帧率调度，因此不会额外加入可能改变用户自定义
固定 FPS 含义的强制限帧。渲染循环仍保持 API 网络操作与 UI/上屏路径分离。
